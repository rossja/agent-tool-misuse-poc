# Copy this file to .env and fill in your OpenAI API key before running.
# The OPENAI_API_KEY will be visible inside the agent container's environment,
# which is intentional — the PWNED banner demonstrates credential exfiltration.

OPENAI_API_KEY=sk-your-key-here
